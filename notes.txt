- typical MVC framework but very modern (for example there is 
  tooling for db migrations, dependency injection (using Service
  Container))

- the dependency injection is very simple and magical. just type
  hint the class to a variable inside an object/class now can use
  it! magic!

- to group and nest routes like I did with CI4, use Route
  Group with prefix

